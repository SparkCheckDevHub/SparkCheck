@* MainLayout.razor *@
@using MudBlazor
@using SparkCheck.Services
@inherits LayoutComponentBase
@inject UserService _userService
@inject UserSessionService UserSession
@inject IJSRuntime JS

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />
<MudPopoverProvider />

<MudLayout Class="main-layout">
    <MudMainContent Class="main-content-with-bottom-nav">
        @Body
    </MudMainContent>

    @if (UserSession.blnIsAuthenticated)
    {
        <BottomNavBar />
    }
</MudLayout>

@* @code {
    protected override void OnInitialized()
    {
        // ======== DEV TEST SESSION SETUP ========
        // Comment this block when integrating real auth/session
        if (!UserSession.blnIsAuthenticated)
        {
            UserSession.intUserID = 1;
            UserSession.strUsername = "TestUser26";
            UserSession.strFirstName = "Johnny";
            UserSession.strLastName = "Test";
            UserSession.strPhone = "1234567890";
            UserSession.dtmDateOfBirth = new DateTime(1991, 2, 10);
            UserSession.intGenderID = 2;
            UserSession.intZipCodeID = 5;
            UserSession.blnIsAuthenticated = true;
            UserSession.blnIsActive = false;
            UserSession.blnIsOnline = false;
            UserSession.blnInQueue = false;
            UserSession.dtmQueuedDate = null;
        }
    }
} *@
